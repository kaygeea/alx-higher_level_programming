#!/usr/bin/node
/* Create then loop through an array of strings, printing each element.
 * CONSTRAINTS:
 * -> "console.log()" must be used to print all outputs.
 * -> Use of "var" is not allowed.
 * -> Use of if/else statement is not allowed.
 * -> "console.log()" can only be used once in the code.
 * -> Use of a loop (while, for, etc) is mandatory.
 */

const strings = ['C is fun', 'Python is cool', 'JavaScript is amazing'];

for (const output of strings) {
  console.log(output);
}
